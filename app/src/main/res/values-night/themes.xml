<resources>
    <!-- Night theme inherits from same base -->
    <style name="Theme.Simonfx" parent="Theme.Material3.DayNight">
        <!-- Night-specific overrides (optional) -->
        <item name="android:colorBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
